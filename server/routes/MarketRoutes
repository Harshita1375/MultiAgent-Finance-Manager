const express = require('express');
const router = express.Router();
const { analyzeMarket } = require('../services/MarketAgent');

router.get('/market-analysis', async (req, res) => {
  const data = await analyzeMarket();
  res.json(data);
});

module.exports = router;